steps:
- name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
  entrypoint: 'bash'
  args:
  - '-c'
  - gsutil -m rsync -d -r ./ gs://${_BUCKET_NAME}/dags

substitutions:
  _BUCKET_NAME: "us-central1-the-last-mile-6a4d6b2f-bucket"
options:
  logging: CLOUD_LOGGING_ONLY
